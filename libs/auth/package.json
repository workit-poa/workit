{
  "name": "@workit/auth",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "node -e \"console.log('auth tests pending')\""
  },
  "dependencies": {
    "@workit/common": "workspace:*"
  }
}

