{
  "name": "@workit/backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "node -e \"console.log('backend tests pending')\""
  },
  "dependencies": {
    "@workit/auth": "workspace:*",
    "@workit/common": "workspace:*",
    "@workit/wallet": "workspace:*",
    "express": "^4.21.2"
  }
}

